#include <iostream>
#include <vector>
#include <string>
#include <cstdlib>
#include "graph.hpp"

int main()
{
    // Exercice 1 (construire un graphe)
    Graph::WeightedGraph graph;
    graph.add_vertex(1);
    graph.add_vertex(2);

    return 0;
}